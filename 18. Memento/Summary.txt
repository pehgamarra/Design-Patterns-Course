Mementos are used to roll back states arbitrarily

A memento is simply a token/handle cass with (tipically) no functions of its own

A memento is not required to expose directly the state to wich it revers the system

Can be use to implement undo/rendo